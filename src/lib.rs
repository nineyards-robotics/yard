//! yard — ROS 2 workspace orchestrator.
//!
//! All real logic will live here as a library; `src/main.rs` is a thin CLI shell.

#[cfg(test)]
mod tests {
    #[test]
    fn library_compiles() {
        // Placeholder. Real tests land alongside real modules in step 2+.
    }
}

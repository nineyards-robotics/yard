# yard

A CLI tool for ROS 2 dev environments and deployment.

## Install for development

With pip:

```sh
python -m pip install -e .
```

With uv:

```sh
uv pip install -e .
```

A CLI entry point will be added later.
